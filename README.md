# rclipd (WIP)

Clipboard manager daemon for Wayland compositors implementing the ext/wlr-data-control protocol.

## Features

* Full clipboard history with multiple mime types
* Sqlite backend with deduplication
* CLI interface over IPC
